on:
    push:
        branches:
        - main
    pull_request:
        branches:
        - main

name: build

jobs:
    lint:
        name: Lint
        runs-on: ubuntu-latest
        steps:
            - name: Checkout source code
              uses: actions/checkout@v6

            - name: Download rust
              uses: dtolnay/rust-toolchain@stable
              with:
                components: clippy

            - name: Install dependencies
              run: sudo apt update && sudo apt install -y build-essential pkg-config libclang-dev cmake libssl-dev
            
            - name: Run clippy linter
              working-directory: post-quantum-client-testing
              run: cargo clippy -- -D warnings
    build:
        name: Build - ${{ matrix.platform.name }}
        # needs: lint
        runs-on: ${{ matrix.platform.os }}
        strategy:
          matrix:
              platform:
                - name: linux-amd64
                  os: ubuntu-latest
                  target: x86_64-unknown-linux-gnu
                
                - name: macos-amd64
                  os: macos-15-intel
                  target: x86_64-apple-darwin

                - name: macos-arm64
                  os: macos-latest
                  target: aarch64-apple-darwin

                - name: windows-amd64
                  os: windows-latest
                  target: x86_64-pc-windows-msvc
        steps:
            - name: Checkout source code
              uses: actions/checkout@v6

            - name: Download rust
              uses: dtolnay/rust-toolchain@stable
              with:
                targets: ${{ matrix.platform.target }}

            - name: Install Linux dependencies
              if: runner.os == 'Linux'
              run: |
                sudo apt update
                sudo apt install -y build-essential pkg-config libclang-dev cmake libssl-dev

            - name: Install MacOS dependencies
              if: runner.os == 'macOS'
              run: |
                brew install openssl@3 cmake
                echo "OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)" >> $GITHUB_ENV
                echo "PKG_CONFIG_PATH=$(brew --prefix openssl@3)/lib/pkgconfig" >> $GITHUB_ENV

            - name: Install Windows dependencies
              if: runner.os == 'Windows'
              run: |
                # Install CMake and OpenSSL (fast, uses pre-built binaries)
                choco install cmake openssl -y
                
                # Use very short target directory to avoid Windows 260 char path limit
                echo "CARGO_TARGET_DIR=C:\t" >> $env:GITHUB_ENV
                
                # Debug: List all OpenSSL directories
                Write-Host "=== Searching for OpenSSL installations ==="
                Get-ChildItem "C:\Program Files" -Directory -Filter "OpenSSL*" -ErrorAction SilentlyContinue | ForEach-Object {
                  Write-Host "Found: $($_.FullName)"
                  if (Test-Path "$($_.FullName)\lib") {
                    Write-Host "  - lib contents:"
                    Get-ChildItem "$($_.FullName)\lib" | ForEach-Object { Write-Host "    $($_.Name)" }
                  }
                }
                
                # Set OpenSSL directory - try multiple locations
                if (Test-Path "C:\Program Files\OpenSSL\lib\VC\x64\MT") {
                  # Static libraries location
                  echo "OPENSSL_DIR=C:\Program Files\OpenSSL" >> $env:GITHUB_ENV
                  echo "OPENSSL_LIB_DIR=C:\Program Files\OpenSSL\lib\VC\x64\MT" >> $env:GITHUB_ENV
                  echo "OPENSSL_INCLUDE_DIR=C:\Program Files\OpenSSL\include" >> $env:GITHUB_ENV
                  echo "OPENSSL_STATIC=1" >> $env:GITHUB_ENV
                } elseif (Test-Path "C:\Program Files\OpenSSL-Win64\lib") {
                  echo "OPENSSL_DIR=C:\Program Files\OpenSSL-Win64" >> $env:GITHUB_ENV
                } elseif (Test-Path "C:\tools\OpenSSL\Win64\lib") {
                  echo "OPENSSL_DIR=C:\tools\OpenSSL\Win64" >> $env:GITHUB_ENV
                } else {
                  Write-Host "WARNING: Could not find OpenSSL lib directory"
                }

            - name: Build project
              working-directory: post-quantum-client-testing
              run: |
                cargo build --release --target ${{ matrix.platform.target }}

        

